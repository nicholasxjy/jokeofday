<% include ../base-top.ejs %>
<% include ../common/header.ejs %>
<div class="fans-section">
    <div class="container">
        <div class="row">
            <% if (locals.fans.length > 0) { %>
                <% locals.fans.forEach(function(fan){ %>
                    <div class="col-md-4 user-info">
                        <div class="info-header">
                            个人信息
                        </div>
                        <div class="info-content">
                            <div class="info-thumbnail">
                                <ul class="list-inline">
                                    <li>
                                        <a href="/user/<%= fan.name %>">
                                            <img src="<%= fan.profile_image_url %>"
                                                 alt="profile thumbnail" title="<%= fan.name %>"
                                                 class="img-circle img-responsive" width="50" height="50"/>
                                        </a>
                                    </li>
                                    <li class="info-username">
                                        <a href="/user/<%= fan.name %>"><%= fan.name %></a>
                                    </li>
                                </ul>
                            </div>
                            <div class="info-data">
                                <ul class="list-inline">
                                    <li title="粉丝"><i class="fa fa-users"></i><%= fan.follower_count %></li>
                                    <li title="关注"><i class="fa fa-heart"></i><%= fan.following_count %></li>
                                    <li title="分数"><i class="fa fa-star"></i><%= fan.score %></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="alert alert-success">
                    <strong>Oops!还没有粉丝，分享点给力的，加油</strong>
                </div>
            <% } %>
        </div>
    </div>
</div>
<% include ../common/footer.ejs %>
<% include ../base-bottom.ejs %>